name: check
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12", cache: pip }
      - run: pip install -r requirements.txt
      - run: python update.py --check        # drift + catch-proven guard tests on a BARE core install; optional extras (unstructured/mcp/pinecone) skip cleanly; no API keys needed
