{
  "generated_from": "real repo state on feat/presentation; every where_configured is a file:line to spot-check",
  "components": [
    {"name": "FastAPI + uvicorn web layer", "role": "HTTP surface (/run, /approve, /trace, dashboards)",
     "where_configured": "api/main.py:10 (app), Dockerfile:7 (uvicorn :8080), demo.sh:45 (local :8000)",
     "status": "present", "note": "port 8080 in container, 8000 for the local demo"},

    {"name": "Docker container", "role": "container image (App Runner / any container host)",
     "where_configured": "deploy/Dockerfile:1-7 (python:3.12-slim, uvicorn CMD)",
     "status": "present", "note": "real buildable Dockerfile; not proven deployed from this repo"},

    {"name": "AWS Lambda + HTTP API (SAM)", "role": "serverless deploy path",
     "where_configured": "deploy/template.yaml:16-33 (AgentFn, Handler api.main.handler), api/main.py handler via Mangum",
     "status": "documented", "note": "SAM template + Mangum handler wired; not built/deployed in this session"},

    {"name": "Anthropic API (model backend)", "role": "default LLM provider",
     "where_configured": "agent/models.py:40-42 (ChatAnthropic), models.py:80 (profile claude-sonnet-api, claude-sonnet-4-5)",
     "status": "present", "note": "the default; used in every live run this session"},

    {"name": "AWS Bedrock (model backend)", "role": "in-account/residency LLM path (Converse)",
     "where_configured": "agent/models.py:43-53 (ChatBedrockConverse), models.py:85-102 (bedrock profiles, us.* inference-profile ids)",
     "status": "present", "note": "on-demand Converse, NOT provisioned throughput; open weights verified invoking per models.py:93 (acct 135359468175, us-west-2); optional Bedrock Guardrail attaches only with a real id (models.py:62-76)"},

    {"name": "Hugging Face endpoint (model backend)", "role": "self-hosted open-weights path",
     "where_configured": "agent/models.py:54-59 (HuggingFaceEndpoint), deploy/hf_endpoint.py",
     "status": "present", "note": "code path present; endpoint URL via HF_ENDPOINT_URL env"},

    {"name": "In-memory BM25 index", "role": "default vector/retrieval backend",
     "where_configured": "agent/retrieval.py (InMemoryIndex), retrieval.py:10 (VECTOR_BACKEND default 'memory')",
     "status": "present", "note": "zero-setup default; hybrid BM25 (+ optional embeddings)"},

    {"name": "Pinecone vector store", "role": "production retrieval backend (swappable)",
     "where_configured": "agent/retrieval.py:110-169 (PineconeIndex), namespace=tenant (retrieval.py:149), PINECONE_INDEX default 'fde-agent' (retrieval.py:112)",
     "status": "stub", "note": "OPTIONAL backend (VECTOR_BACKEND=pinecone); pinecone package imported lazily; namespace-per-tenant isolation; not exercised live this session (in-memory is default). ACL parity guarded by D-037"},

    {"name": "Unstructured.io ingestion", "role": "real document parsing → chunk_document",
     "where_configured": "agent/ingest.py:2 (Unstructured.io, Apache-2.0), optional dep (requirements-ingest.txt)",
     "status": "present", "note": "optional dep; verified on 3 NIST PDFs → 615 chunks (docs/INGESTION-PROOF.md); core runs without it"},

    {"name": "LangSmith tracing", "role": "agent trace (prompts, tool I/O, node path)",
     "where_configured": "agent/tracing.py:7 (env LANGSMITH_TRACING/API_KEY/PROJECT), tracing.py:53 (Client), deploy/template.yaml:27 (LANGSMITH_TRACING=true)",
     "status": "present", "note": "opt-in via env; observability only, NOT a durable compliance log (see logs.json)"},

    {"name": "Braintrust evals", "role": "graded eval history + judge agreement/calibration",
     "where_configured": "evals/run_evals.py:16,37 (Eval push), evals/harness.py:25 (BRAINTRUST_API_KEY gate)",
     "status": "present", "note": "opt-in via key; the offline gate (evals/gate.py, evals/runner.py) does NOT depend on it"},

    {"name": "AWS Secrets Manager", "role": "ANTHROPIC_API_KEY at cold start (Lambda)",
     "where_configured": "api/main.py:12-17 (boto3 get_secret_value), deploy/template.yaml:31 (secretsmanager:GetSecretValue)",
     "status": "present", "note": "Lambda path only; local uses .env"}
  ]
}
