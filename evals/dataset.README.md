# SLOT 1: GOLDEN SET (evals/dataset.jsonl)

Write 5-8 cases for the new domain FIRST — this is the spec, before the code that satisfies it.
Include an adversarial slice (injection / PII / cross-tenant / out-of-policy). Row = {input, expected, tags};
tags[0] is the slice. The two rows here are worked examples — replace them.
